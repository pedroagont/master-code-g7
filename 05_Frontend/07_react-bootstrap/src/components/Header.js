import Navigation from './Navigation';
import MainCarousel from './MainCarousel';

function Header() {
  return (
    <header>
      <Navigation />
      <MainCarousel />
    </header>
  );
}

export default Header;
